mode: hybrid

data:
  data_path: ./data/acl2017
  max_events: 0

model:
  tda_dim: 128
  transformer_model: bert-base-uncased
  text_dim: 768
  fusion_dim: 512
  num_classes: 4

training:
  batch_size: 16
  learning_rate: 1e-4
  epochs: 20
  device: cuda
  checkpoint_dir: ./checkpoints/hybrid
